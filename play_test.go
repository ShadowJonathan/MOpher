package main

/*import (
	"fmt"
	"testing"
)

func TestBlocks(t *testing.T) {
	initBlocks()
	for i := 0; i < 212; i++ {
		//I := ItemById(i)
		//fmt.Println(i, I.Name())
		b := GetBlockByCombinedID(uint16(i * 16))
		fmt.Println(i, b.String(), b.Hardness())
	}
}
*/
